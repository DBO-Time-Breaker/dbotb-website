<!DOCTYPE html>
<html lang="en" class="h-full bg-white">
  <head>
    <title><%= content_for?(:app_title) ? yield(:app_title) : 'DBO Time Breaker'  %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: 'all' %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="h-full relative">
    <%= render 'web/shared/navbar' %>

    <% if notice %>
     <p class="bg-green-500 text-white text-center p-4 text-lg border-solid border-white absolute top-0 left-0 w-full"><%= notice.html_safe %></p>
    <% end %>
    <% if alert %>
     <p class="bg-red-500 text-white text-center p-4 text-lg border-solid border-white absolute top-0 left-0 w-full"><%= alert.html_safe %></p>
    <% end %>

    <div class="mx-auto container py-8">
      <%= yield %>
    </div>

    <%= yield :extra_javascripts %>
  </body>
</html>
